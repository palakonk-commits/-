# 💰 Slip TopUp — ระบบเติมเงินผ่านสลิปโอนเงินอัตโนมัติ

ระบบตรวจสอบสลิปโอนเงินผ่าน QR Code แล้วเติมเงินเข้าระบบอัตโนมัติ  
ใช้ [Slip2Go API](https://app.slip2go.com) ในการดึงข้อมูลสลิป

---

## 📑 สารบัญ

- [ภาพรวมระบบ](#-ภาพรวมระบบ)
- [ติดตั้ง](#-ติดตั้ง)
- [ตั้งค่า .env](#-ตั้งค่า-env)
  - [Slip2Go API](#1-slip2go-api)
  - [ข้อมูลหน้าเว็บ](#2-ข้อมูลหน้าเว็บ)
  - [ตั้งค่าตรวจสอบผู้รับ (สำคัญมาก!)](#3-ตั้งค่าตรวจสอบผู้รับ-สำคัญมาก)
- [Flowchart การตรวจสอบสลิป](#-flowchart-การตรวจสอบสลิป)
- [ข้อผิดพลาดที่พบบ่อย & วิธีแก้](#-ข้อผิดพลาดที่พบบ่อย--วิธีแก้)
- [ตัวอย่าง .env ที่ถูกต้อง](#-ตัวอย่าง-env-ที่ถูกต้อง)
- [API Endpoints](#-api-endpoints)
- [โครงสร้างโปรเจค](#-โครงสร้างโปรเจค)

---

## 🔍 ภาพรวมระบบ

```
ผู้ใช้อัพโหลดสลิป
        ↓
Server อ่าน QR Code จากรูป
        ↓
ส่ง QR Data ไป Slip2Go API
        ↓
ได้ข้อมูลสลิปกลับมา (ผู้โอน, ผู้รับ, จำนวนเงิน)
        ↓
Server เช็คเองว่า:
  ✅ สลิปซ้ำไหม?
  ✅ ผู้รับตรงกับบัญชีเราไหม? (เทียบ 4 ตัวท้าย + ชื่อ)
        ↓
ผ่านทุกเงื่อนไข → เติมเงินสำเร็จ!
```

---

## 🚀 ติดตั้ง

### ความต้องการ
- Node.js v16+
- บัญชี [Slip2Go](https://app.slip2go.com) พร้อม API Key (มีเครดิตเพียงพอ)

### ขั้นตอน

```bash
# 1. Clone หรือดาวน์โหลดโปรเจค
cd slip-topup-website

# 2. ติดตั้ง dependencies
npm install

# 3. สร้างไฟล์ .env (คัดลอกจากตัวอย่างด้านล่าง)
# ⚠️ ต้องตั้งค่าให้ครบก่อนรัน!

# 4. รันเซิร์ฟเวอร์
npm start

# 5. เปิดเว็บ
# http://localhost:3000
```

---

## ⚙️ ตั้งค่า .env

สร้างไฟล์ `.env` ที่ root ของโปรเจค มีทั้งหมด 3 ส่วน:

### 1. Slip2Go API

| ค่า | คำอธิบาย | ตัวอย่าง |
|-----|---------|---------|
| `SLIP2GO_API_KEY` | API Key จากหน้า Authentication ของ Slip2Go | `1Tc2surcvOyvLaotLykx...` |
| `SLIP2GO_API_URL` | **Base URL เท่านั้น** (ห้ามใส่ path endpoint!) | `https://connect.slip2go.com` |
| `PORT` | พอร์ตที่ server จะรัน | `3000` |

> ⚠️ **ห้ามใส่ path ต่อท้าย** `SLIP2GO_API_URL`  
> ❌ `https://connect.slip2go.com/api/verify-slip/qr-code/info`  
> ✅ `https://connect.slip2go.com`  
> เพราะ server จะต่อ path `/api/verify-slip/qr-code/info` ให้เองอัตโนมัติ

### 2. ข้อมูลหน้าเว็บ

| ค่า | คำอธิบาย | ตัวอย่าง |
|-----|---------|---------|
| `SITE_NAME` | ชื่อเว็บไซต์ | `MyTopUp` |
| `BANK_NAME` | ชื่อธนาคาร (แสดงบนหน้าเว็บ) | `ธนาคารกสิกรไทย` |
| `BANK_ACCOUNT_NUMBER` | เลขบัญชีหรือเบอร์พร้อมเพย์ (แสดงบนหน้าเว็บ) | `0612418649` |
| `BANK_ACCOUNT_NAME` | ชื่อบัญชี (แสดงบนหน้าเว็บ) | `พลากร คงแจ่ม` |

> 💡 ค่าเหล่านี้แสดงบนหน้าเว็บเท่านั้น ไม่มีผลกับการตรวจสอบสลิป

### 3. ตั้งค่าตรวจสอบผู้รับ (สำคัญมาก!)

| ค่า | คำอธิบาย | ตัวอย่าง |
|-----|---------|---------|
| `RECEIVER_ACCOUNT_LAST4` | **4 ตัวท้าย**ของเลขบัญชี/เบอร์พร้อมเพย์ (คั่นด้วย `,`) | `9966,8649` |
| `RECEIVER_NAMES` | ชื่อผู้รับที่ยอมรับ (คั่นด้วย `,`) | `PALAKON,พลากร` |

#### 🔴 กฎสำคัญ: ใส่แค่ 4 ตัวท้ายเท่านั้น!

```
                    ทำไมต้อง 4 ตัวท้าย?
┌─────────────────────────────────────────────────────┐
│                                                     │
│  เพราะ QR Code ในสลิปจะ mask เลขบัญชีไว้            │
│  แสดงแค่ 4 ตัวท้ายเท่านั้น เช่น                      │
│                                                     │
│  บัญชีจริง:  073-1-29966-8                          │
│  ใน QR:      xxx-x-x9966-x   ← เห็นแค่ 9966        │
│                                                     │
│  เบอร์จริง:  061-241-8649                           │
│  ใน QR:      xxx-xxx-8649     ← เห็นแค่ 8649        │
│                                                     │
│  ถ้าใส่เลขเต็ม → ระบบเทียบไม่ตรง → ปฏิเสธทุกสลิป!   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**ตัวอย่างการใส่ `RECEIVER_ACCOUNT_LAST4`:**

| สถานการณ์ | ค่าที่ถูกต้อง | ค่าที่ผิด |
|-----------|-------------|----------|
| บัญชีกสิกร `073-1-29966-8` | `9966` | ❌ `0731299668` |
| พร้อมเพย์เบอร์ `061-241-8649` | `8649` | ❌ `0612418649` |
| มีหลายบัญชี | `9966,8649` | ❌ `0731299668,0612418649` |

**ตัวอย่างการใส่ `RECEIVER_NAMES`:**

| สถานการณ์ | ค่าที่ถูกต้อง |
|-----------|-------------|
| ชื่ออังกฤษ | `PALAKON` |
| ชื่อไทย | `พลากร` |
| รองรับทั้งสองภาษา | `PALAKON,พลากร` |

> 💡 ระบบเช็คชื่อแบบ **contains** ไม่สน **ตัวพิมพ์ใหญ่/เล็ก**  
> เช่น ใส่ `PALAKON` จะ match กับ `MASTER PALAKON K`, `Mr. Palakon Kongjam` ฯลฯ  
> ห้ามใส่คำนำหน้า เช่น `MR.` หรือ `MASTER` เพราะบางสลิปอาจไม่มี

---

## 📊 Flowchart การตรวจสอบสลิป

```
ผู้ใช้อัพโหลดสลิป
         │
         ▼
┌─────────────────┐    ไม่มี
│  มีรูปสลิปไหม?  │──────────→ ❌ "กรุณาอัพโหลดรูปสลิป"
└────────┬────────┘
         │ มี
         ▼
┌─────────────────┐    ไม่ได้กรอก
│ กรอก User ID?   │──────────→ ❌ "กรุณากรอก User ID"
└────────┬────────┘
         │ กรอกแล้ว
         ▼
┌─────────────────┐    อ่านไม่ได้
│ อ่าน QR ได้ไหม? │──────────→ ❌ "ไม่พบ QR Code ในรูปภาพ"
└────────┬────────┘
         │ อ่านได้
         ▼
┌─────────────────────┐
│ ส่ง QR → Slip2Go API│
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐    "200404"
│ Slip2Go ตอบอะไร?    │──────────→ ❌ "ไม่พบข้อมูลสลิป"
└────────┬────────────┘
         │ "200000" (สำเร็จ)
         ▼
┌─────────────────┐    มี transRef ซ้ำ
│ สลิปซ้ำไหม?     │──────────→ ❌ "สลิปนี้ถูกใช้งานไปแล้ว"
│ (เช็คใน memory) │
└────────┬────────┘
         │ ไม่ซ้ำ
         ▼
┌──────────────────────────┐    4 ตัวท้ายไม่ตรง
│ เลขบัญชีผู้รับ 4 ตัวท้าย  │──────────→ ❌ "ไม่ได้โอนเข้าบัญชีของเรา"
│ ตรงกับ RECEIVER_ACCOUNT_ │
│ LAST4 ไหม?               │
└────────┬─────────────────┘
         │ ตรง
         ▼
┌──────────────────────────┐    ชื่อไม่ตรง
│ ชื่อผู้รับตรงกับ           │──────────→ ❌ "ไม่ได้โอนเข้าบัญชีของเรา"
│ RECEIVER_NAMES ไหม?      │
└────────┬─────────────────┘
         │ ตรง
         ▼
   ✅ เติมเงินสำเร็จ!
   บันทึก transaction
   อัพเดทยอดเงิน
```

---

## ❌ ข้อผิดพลาดที่พบบ่อย & วิธีแก้

### 1. 🔴 `Recipient account Mismatch` / ผู้รับไม่ตรง

**สาเหตุ:** ใส่เลขบัญชีหรือเบอร์พร้อมเพย์แบบเต็มๆ ใน config

**อาการ:** ทุกสลิปที่อัพโหลดจะขึ้น "ไม่ได้โอนเข้าบัญชีของเรา" ทั้งที่โอนเข้าจริง

**วิธีแก้:**
```env
# ❌ ผิด — ใส่เลขเต็ม
RECEIVER_ACCOUNT_LAST4=0731299668,0612418649

# ✅ ถูก — ใส่แค่ 4 ตัวท้าย
RECEIVER_ACCOUNT_LAST4=9966,8649
```

**เหตุผล:** QR Code ในสลิปโอนเงิน จะ mask เลขบัญชีไว้แสดงแค่ 4 ตัวท้าย ดังนั้นถ้าเราใส่เลขเต็มไปเทียบ มันจะไม่มีทางตรงกัน

---

### 2. 🔴 `Slip is Duplicated` ทั้งที่ยังไม่เคยเติมเงินสำเร็จ

**สาเหตุ:** เคยใช้ `checkCondition.checkDuplicate` ส่งไปฝั่ง Slip2Go ก่อนหน้านี้ ทำให้ Slip2Go จำว่าสลิปนี้ "เคยถูกเช็คแล้ว" แม้ว่าผลจะไม่ผ่าน (Mismatch) ก็ตาม

**อาการ:** ส่งสลิปครั้งแรก → Mismatch → ส่งซ้ำ → Duplicated → ไม่มีทางผ่านเลย

**วิธีแก้ (ป้องกัน):**
- **ไม่ส่ง `checkCondition`** ไปให้ Slip2Go (ระบบปัจจุบันแก้แล้ว)
- เช็คสลิปซ้ำเองฝั่ง server ด้วย `transRef` ใน memory แทน
- ⚠️ สลิปที่เคยส่ง checkDuplicate ไปแล้ว จะ Duplicated ถาวรใน Slip2Go ต้องใช้สลิปใหม่

---

### 3. 🔴 URL ซ้ำซ้อน — API ตอบ 404

**สาเหตุ:** ใส่ `SLIP2GO_API_URL` เป็น full path

**อาการ:** URL กลายเป็น `https://connect.slip2go.com/api/verify-slip/qr-code/info/api/verify-slip/qr-code/info`

**วิธีแก้:**
```env
# ❌ ผิด — ใส่ path endpoint ด้วย
SLIP2GO_API_URL=https://connect.slip2go.com/api/verify-slip/qr-code/info

# ✅ ถูก — base URL เท่านั้น
SLIP2GO_API_URL=https://connect.slip2go.com
```

---

### 4. 🔴 `Slip found.` แต่ขึ้น Error

**สาเหตุ:** Slip2Go ตอบ `code: "200000"` (string) แต่โค้ดเช็ค `code === 200` (number)

**วิธีแก้:** ปัจจุบันแก้แล้ว — ใช้ `String(apiData.code) === '200000'`

---

### 5. 🔴 ชื่อผู้รับไม่ตรงทั้งที่ถูกบัญชี

**สาเหตุ:** ใส่คำนำหน้าชื่อ หรือใส่ชื่อเต็มที่อาจไม่ตรงกับใน API

**อาการ:** `RECEIVER_NAMES=MR. PALAKON KONGJAM` → ไม่ match กับ `MASTER PALAKON K`

**วิธีแก้:**
```env
# ❌ ผิด — ใส่คำนำหน้า + ชื่อเต็ม
RECEIVER_NAMES=MR. PALAKON KONGJAM

# ✅ ถูก — ใส่แค่ชื่อหลัก (ไม่มีคำนำหน้า)
RECEIVER_NAMES=PALAKON,พลากร
```

> 💡 API อาจตอบกลับชื่อคนละรูปแบบ เช่น `MASTER PALAKON K` หรือ `MR. PALAKON KONGJAM`  
> ดังนั้นใส่แค่ `PALAKON` จะ match ได้ทุกรูปแบบ

---

### 6. 🟡 เครดิต Slip2Go หมด

**อาการ:** API ตอบ HTTP 402

**วิธีแก้:** เข้า [app.slip2go.com](https://app.slip2go.com) เติมเครดิต (API ใช้เครดิตทุกครั้งที่เรียก)

---

## 📄 ตัวอย่าง .env ที่ถูกต้อง

```env
# ================================
# Slip2Go API Configuration
# ================================

# API Key จาก slip2go.com (ดูได้ที่หน้า Authentication)
SLIP2GO_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Base URL เท่านั้น (ห้ามใส่ /api/verify-slip/... ต่อท้าย!)
SLIP2GO_API_URL=https://connect.slip2go.com

# Server Port
PORT=3000

# ชื่อร้าน/เว็บไซต์
SITE_NAME=MyTopUp

# บัญชีธนาคารรับเงิน (แสดงบนหน้าเว็บเท่านั้น)
BANK_NAME=ธนาคารกสิกรไทย
BANK_ACCOUNT_NUMBER=073-1-29966-8
BANK_ACCOUNT_NAME=พลากร คงแจ่ม

# ===== ตรวจสอบผู้รับ (ส่วนที่สำคัญที่สุด!) =====

# 4 ตัวท้ายของเลขบัญชี/เบอร์พร้อมเพย์
# บัญชีกสิกร 073-1-29966-8 → 9966
# เบอร์พร้อมเพย์ 061-241-8649 → 8649
RECEIVER_ACCOUNT_LAST4=9966,8649

# ชื่อผู้รับ (ใส่ชื่อหลัก ไม่ต้องมีคำนำหน้า)
RECEIVER_NAMES=PALAKON,พลากร
```

---

## 🔌 API Endpoints

| Method | Path | คำอธิบาย |
|--------|------|---------|
| `GET` | `/api/bank-info` | ข้อมูลบัญชีธนาคาร (แสดงบนหน้าเว็บ) |
| `GET` | `/api/balance?userId=xxx` | ดูยอดเงินของ user |
| `GET` | `/api/transactions?userId=xxx` | ดูประวัติการเติมเงินของ user |
| `POST` | `/api/verify-slip` | อัพโหลดสลิปเพื่อตรวจสอบและเติมเงิน |

### POST `/api/verify-slip`

**Content-Type:** `multipart/form-data`

| Field | Type | คำอธิบาย |
|-------|------|---------|
| `slip` | File | ไฟล์รูปสลิป (JPG, PNG, WEBP ≤ 5MB) |
| `userId` | String | User ID หรือเบอร์โทร |

**Response สำเร็จ:**
```json
{
  "success": true,
  "message": "เติมเงินสำเร็จ 100.00 บาท",
  "data": {
    "id": "TXN-1234567890",
    "transRef": "016054151417CTF04614",
    "amount": 100,
    "senderName": "MASTER PALAKON K",
    "receiverName": "MR. PALAKON K",
    "transDate": "2026-02-23T15:14:17+07:00"
  }
}
```

**Response ไม่สำเร็จ:**
```json
{
  "success": false,
  "message": "สลิปนี้ไม่ได้โอนเข้าบัญชีของเรา กรุณาโอนเงินไปยังบัญชีที่แสดงบนหน้าเว็บ"
}
```

---

## 📁 โครงสร้างโปรเจค

```
slip-topup-website/
├── .env                 ← ตั้งค่า API Key, บัญชี, ผู้รับ
├── server.js            ← Express server + ตรวจสอบสลิป
├── package.json
├── public/
│   ├── index.html       ← หน้าเว็บหลัก
│   ├── app.js           ← Frontend logic
│   └── style.css        ← Styling
└── uploads/             ← เก็บรูปสลิปชั่วคราว (ลบอัตโนมัติหลังเช็ค)
```

---

## 🛡️ ข้อควรระวัง

1. **ข้อมูลใน memory** — transactions และ usedSlips เก็บใน RAM ถ้า restart server จะหายหมด → ควรต่อ database ในอนาคต
2. **API Key** — ห้ามเปิดเผย! ใส่ใน `.env` และเพิ่ม `.gitignore`
3. **เครดิต Slip2Go** — ทุกครั้งที่เรียก API จะใช้เครดิต ตรวจสอบยอดเครดิตบ่อยๆ
4. **สลิปที่เคย checkDuplicate แล้ว** — ถ้าเคยส่ง `checkCondition` ไป Slip2Go มาก่อน สลิปนั้นจะถูกนับว่าใช้แล้วถาวร ใช้ซ้ำไม่ได้

---

## 📜 License

MIT
